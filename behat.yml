default:
    context:
        class: PlayerContext
    extensions:
        Behat\MinkExtension\Extension:
            base_url: http://localhost/chess
            goutte: ~
            default_session: symfony2
        SensioLabs\Behat\PageObjectExtension\Extension: ~
        Behat\Symfony2Extension\Extension: ~
